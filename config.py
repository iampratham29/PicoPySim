# config.py

# Switch mode: "SIM" or "HARDWARE"
MODE = "SIM"

# Serial port for hardware mode (use `COMx` on Windows or `/dev/ttyACM0` on Linux)
SERIAL_PORT = "/dev/ttyACM0"
BAUDRATE = 115200
TIMEOUT = 2
